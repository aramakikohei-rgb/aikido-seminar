# Turso Database Configuration
# Production: get these from `turso db show <db-name>` and `turso db tokens create <db-name>`
TURSO_DATABASE_URL=libsql://aikido-seminar-xxx.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Local Development: use local SQLite file (no auth token needed)
# TURSO_DATABASE_URL=file:./local.db
